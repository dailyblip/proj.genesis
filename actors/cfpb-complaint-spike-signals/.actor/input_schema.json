{
  "title": "CFPB Complaint Spike Signals",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "currentDays": {"title": "Current window (days)", "type": "integer", "default": 7, "minimum": 1, "maximum": 30},
    "baselineDays": {"title": "Baseline window (days)", "type": "integer", "default": 28, "minimum": 7, "maximum": 120},
    "minCurrentComplaints": {"title": "Minimum current complaints", "type": "integer", "default": 10, "minimum": 1},
    "minSpikeRatio": {"title": "Minimum spike ratio", "type": "number", "default": 2.0, "minimum": 1.0},
    "maxSignals": {"title": "Maximum signals", "type": "integer", "default": 50, "minimum": 1, "maximum": 500}
  }
}
